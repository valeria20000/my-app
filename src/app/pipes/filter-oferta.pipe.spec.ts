import { FilterOfertaPipe } from './filter-oferta.pipe';

describe('FilterOfertaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOfertaPipe();
    expect(pipe).toBeTruthy();
  });
});
